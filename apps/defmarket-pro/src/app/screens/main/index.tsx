import React from 'react';
import BottomTabs from '../../components/molecules/bottom-navigation/BottomTabs';

const index = (props: any) => {
    return <BottomTabs {...props} />;
};

export default index;
