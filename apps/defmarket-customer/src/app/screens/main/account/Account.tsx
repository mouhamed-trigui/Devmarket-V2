import React from 'react';
import { Text, View } from 'react-native';
export default function Account(props: any) {
    return (
        <View>
            <Text>Account</Text>
        </View>
    );
}
